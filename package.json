{
  "private": true,
  "scripts": {
    "start": "http-server -o",
    "build": "mkdir -p dist/ && inliner -i --skip-absolute-urls index.html > dist/index.html && cp -vr downloads img favicon.ico dist/"
  },
  "dependencies": {
    "bigfishtv-turret": "^3.3.1",
    "ideal-image-slider": "^1.5.1",
    "multi-date-picker": "^2.0.9",
    "smooth-scroll": "github:cferdinandi/smooth-scroll"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "inliner": "^1.9.4"
  }
}
